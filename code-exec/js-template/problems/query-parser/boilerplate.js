/**
 * Parses a query string URL into an object.
 * @param {string} queryString - The query string (e.g., "?name=John&age=30")
 * @returns {Object} - Key-value pairs extracted from the query string
 */
function parseQueryString(queryString) {
    // TODO: Implement the parser
    // Note: Do not use URLSearchParams or external libraries. Implement the logic manually.
}

module.exports = parseQueryString;
